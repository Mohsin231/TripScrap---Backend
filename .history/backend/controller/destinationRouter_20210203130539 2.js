const express = require("express");
const router = express.Router();
const destination = require("../models/destination");
const triposo

router.get("/", (req, res, next) => {

})

// user model, after enters info, asked question, "where are you going?", after enter input field, the input field will fetch data from destinationRouter/Schema,...

// destination grabs from Triposo API, then Triposo API & destination 
// will send data to TripScrap.JS

// input field will trigger destinationRouter & destinationSchema,
// destinationRouter will pull from Triposo API
// 
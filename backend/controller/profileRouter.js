const express = require("express");
const router = express.Router();
const User = require("../models/profile");

//in the backend the code will finish the funcitonality. for every single card for api make like button
//user.save.push(e.current.target)
//if button is clicked then push to user target 
//give it a route 